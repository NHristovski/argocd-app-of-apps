apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monitoring-extensions.fullname" . }}-test-connection"
  labels:
    {{- include "monitoring-extensions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monitoring-extensions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
